public class InputValidator {
    // Task: Method to validate non-empty string (for names, course titles)
    // Task: Method to validate numeric studentId (fixed length/range if required)
    // Task: Method to validate course code format (e.g., 2–4 letters + 3 digits)
    // Task: Method to validate credits/section as positive integers within range
    // Task: Method to validate file path exists and is readable
    // Task: (Optional) Method to validate menu choice within allowed options
    // Task: (Optional) Aggregate method to validate a Registration object
}
